{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.bulatko/vk",
  "description": "MCP server for VK (VKontakte) API: users, walls, groups, friends, newsfeed, photos, likes.",
  "repository": {
    "url": "https://github.com/bulatko/vk-mcp-server.git",
    "source": "github"
  },
  "version": "0.3.1",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "vk-mcp-server",
      "version": "0.3.1",
      "runtimeHint": "npx",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "VK API access token, issued by your own Standalone app (create one at https://vk.com/editapp?act=create). See the README for the authorisation URL. A token is bound to the app that issued it, so use your own rather than a shared app ID.",
          "isRequired": true,
          "isSecret": true,
          "name": "VK_ACCESS_TOKEN"
        }
      ]
    }
  ]
}
